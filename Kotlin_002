ryjytynhfh
fun main() {

    val age = 5 * 365
    val name = "Дружище"

    println("С днем рождения, ${name}!")

    // Let's print a cake!
    println("   ,,,,,   ")
    println("   |||||   ")
    println(" =========")
    println("@@@@@@@@@@@")
    println("{~@~@~@~@~}")
    println("@@@@@@@@@@@")

    // This prints an empty line.
    println("")

    println("Тебе уже ${age} столько дней, ${name}!")
    println("${age} день и ты совсем большой!")
}
//
внутри main()функции добавьте следующий код, переменную ageс именем и значением 5.
(Вы должны поместить эту строку перед println()утверждениями).
val это специальное слово, используемое Kotlin, называемое ключевым словом , указывающее,
что следующее за ним имя переменной.
age это имя переменной.
= делает значение age(слева) таким же, как значение справа.

Чтобы использовать переменную внутри оператора печати, вам нужно окружить ее некоторыми символами,
которые сообщают системе, что дальше идет не текст, а переменная.
Вместо того, чтобы печатать текст, система должна напечатать значение переменной.
Вы делаете это, помещая свою переменную в фигурные скобки, которым предшествует знак доллара.
${variable}
В своем коде замените число 5 в обоих операторах печати на age переменную, как показано ниже.

println("You are already ${age}!")
println("${age} is the very best age to celebrate!")
//
